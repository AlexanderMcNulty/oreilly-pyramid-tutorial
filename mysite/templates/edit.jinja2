{%  extends "base.jinja2" %}
{%  block title %}ToDos Edit{% endblock %}
{%  block content %}


{# view, i think is a hard coded name in order to get view properties, this could be a good question for Paul/stackover flow #}
{#  <h1>{{ view.current }}</h1> #}

<h4>Todos - edit: {{ todo.id }}</h4>

    <div> {{ edit_form | safe }}</div>

{#
<form method="POST">
    <input name="new_title" value="{{ todo.title }}"/>
    <!-- this will trigger the POST decorator, edit_handler() in views.py -->
    <input type="submit" name="form.submit"/>
    <!-- this will trigger the default decorator, edit() in views.py -->
    <input type="submit" name="form.cancel" value="Cancel"/>
</form>
#}

{% endblock %}
